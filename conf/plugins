# a plugin should be registered as a function.
# if your plugin have several functions, you can source the script file like module plugin.
# exit should never used to terminate function(and is disabled), please use return instead.

# This is a test plugin
test(){
    "${VMS_HOME}"/plugin/test.sh $@
}

# module plugin
. "${VMS_HOME}"/plugin/module.sh